<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.back.member.dao">
	<select id="idChk" parameterType="com.care.back.member.dto.MemberDto">
		SELECT COUNT(*) FROM bil.member WHERE ID = #{id}
	</select>
</mapper>